name: Keep Render Service Alive

on:
  schedule:
    - cron: "*/14 * * * *"   # every 14 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Subdomain
        run: |
          curl -o /dev/null -sS -w "Subdomain: %{http_code}\n" "https://dearnco-django.onrender.com" || true

      - name: Ping Custom Domain (dearnco.is-a.dev)
        run: |
          curl -o /dev/null -sS -w "Custom (dearnco): %{http_code}\n" "https://dearnco.is-a.dev" || true

      - name: Ping Custom Domain (dear.is-a.dev)
        run: |
          curl -o /dev/null -sS -w "Custom (dear): %{http_code}\n" "https://dear.is-a.dev" || true
